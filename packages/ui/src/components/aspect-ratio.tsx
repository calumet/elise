/**
 * Reserva una caja con una proporción fija.
 *
 * @module
 */

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio";

/** Reserva una caja con la proporción que pidas, para que el contenido no mueva el layout al cargar. */
export const AspectRatio = AspectRatioPrimitive.Root;
